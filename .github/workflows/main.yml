name: Quiz App CI/CD Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Verify files
      run: echo "Files uploaded successfully"

    - name: Build step
      run: echo "No build required for static frontend"

  deploy:
    needs: build
    runs-on: ubuntu-latest

    steps:
    - name: Deploy step
      run: echo "Deployment in progress..."
